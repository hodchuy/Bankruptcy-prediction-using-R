df.ANOVA <- read.csv("t-1 ANOVA.csv")
scale.ANOVA <- scale(df.ANOVA[1:5])
var(scale.ANOVA[,1])
df.final.ANOVA <- cbind(scale.ANOVA,df.ANOVA[6])
library(caTools)
set.seed(101)
split.ANOVA = sample.split(df.final.ANOVA$br, SplitRatio = 0.7)
train.ANOVA = subset(df.final.ANOVA, split == TRUE)
test.ANOVA = subset(df.final.ANOVA, split == FALSE)
str(train.ANOVA)
library(neuralnet)
nn.ANOVA <- neuralnet(br ~ b + c + d + e + f, data = train.ANOVA, hidden = 5, linear.output = FALSE)
predicted.nn.ANOVA <- compute(nn.ANOVA, test[,1:5])
predictions.ANOVA <- sapply(predicted.nn.ANOVA$net.result, round)
table(predictions.ANOVA,test.ANOVA$br)

